# SigFilterForge — руководство

Приложение считает фильтры: вы задаёте, ЧЕГО хотите (полосы, неравномерность,
подавление), нажимаете «Рассчитать» — и получаете готовый фильтр: коэффициенты
или номиналы деталей, характеристики графиками, чертёж схемы и словесное
описание того, что получилось.

Работать можно и не зная методов расчёта: у каждого класса фильтров есть
вменяемые умолчания, а вкладка «Описание» объясняет полученный фильтр словами.

---

## 1. Запуск

```
./sigfilterforge                 окно
./sigfilterforge проект.sff      окно с загруженным проектом
./sigfilterforge --selftest      внутренние проверки, без окна
```

Нужен Linux x86-64 и SDL2. Рядом с бинарём должен лежать каталог `assets` —
из него берётся шрифт; больше приложению ничего не нужно. Каталог настроек
(размер окна, масштаб шрифта) подменяется переменной `SFF_CONFIG_DIR`.

## 2. Окно

Слева — **панель параметров**, она перестраивается под выбранный класс фильтра:
у КИХ спрашивают частоту дискретизации и длину, у LC — сопротивление источника
и порядок, у Калмана — модель и СКО. Лишнего не показывается.

Справа — **вкладки** с результатом. Внизу — **строка состояния**: слева то, что
получилось («достигнуто: пульсация 0.789 дБ, подавление 62.0 дБ (N=28)») или
почему не получилось, справа — имя открытого проекта.

Меню: **Файл** (Новый, Открыть, Сохранить, Сохранить как, Выход), **Сигнал**
(параметры испытательного сигнала), **Справка**.

Считать — кнопка «Рассчитать» или **Enter**.

## 3. Что умеет считать

| Класс | Что получите | Метод |
|---|---|---|
| **КИХ (цифровой)** | коэффициенты `h[n]` | Паркс-Маклелланд (обмен Ремеза) — равномерные пульсации; и скользящее среднее |
| **БИХ (цифровой)** | биквадные секции | аналоговый прототип → билинейное преобразование; и экспоненциальное сглаживание |
| **LC (пассивный)** | номиналы катушек и конденсаторов | метод вносимых потерь: лестница либо связанные контуры |
| **ОУ (активный RC)** | номиналы R и C по каскадам | полюса прототипа → каскады Саллен-Ки / MFB (Рауха) |
| **временные (сглаживание)** | оценка по времени, без АЧХ | медианный, альфа-бета-гамма, Калман, ALE (гребёнка) |

**Характеристики:** ФНЧ, ФВЧ, ПФ (полосовой), РФ (режекторный), Гильберт (КИХ).

**Аппроксимации:** Баттерворт, Чебышёв I, Чебышёв II (БИХ), эллиптическая
(Кауэра, БИХ). У LC и ОУ — всеполюсные: Баттерворт и Чебышёв I.

**Исполнение LC** выбирается отдельно: лестница, либо связанные контуры —
ёмкостями связи, автотрансформаторной связью или C-делителями. У лестницы
задаётся ещё, каким элементом она начинается (поперечным или продольным).

**Модели Калмана:** без модели (кинематика «положение-скорость-ускорение»),
синусоида с заданной частотой, синусоида с плавающей частотой, синусоида с
плавающими частотой и амплитудой.

## 4. Как работать

1. Выберите **Класс** и **Характеристику** — панель параметров перестроится.
2. Задайте частоты. У ФНЧ/ФВЧ это `Fпроп` и `Fзадерж`, у полосовых — четыре
   частоты, у аналоговых — просто частота среза (или нижняя и верхняя).
3. Задайте требования: **пульсация Ап** в полосе пропускания и **подавление
   Ас** в полосе задерживания, в децибелах.
4. Оставьте **длину/порядок = 0**, чтобы программа подобрала минимальный сама.
   Поставьте своё число, если порядок задан извне.
5. **Рассчитать** (или Enter). Строка состояния скажет, что достигнуто.
6. Смотрите вкладки, правьте параметры, считайте снова — расчёт мгновенный.
7. **Файл → Сохранить как…** — проект `.sff`.

**Отказы громкие и адресные.** Если требование невыполнимо, программа скажет
чем именно: «длины 5 не хватает: подавление 35.7 дБ вместо 60.0 — добавьте
отводов», «усиление в центре меньше 0 дБ: каскаду MFB не хватает 2Q²>A0».
Молчаливой подмены параметров нет: программа не станет считать не то, что
просили, не сказав об этом.

## 5. Вкладки

Набор вкладок зависит от класса — лишних не бывает.

- **Структура** (цифровые) — чертёж фильтра на узлах задержки, сумматорах и
  умножителях: видно, сколько чего понадобится в железе.
- **Схема** (LC и ОУ) — чертёж с номиналами деталей. У ОУ под каждым каскадом
  подписаны его частота `f0` и добротность `Q`.
- **АЧХ**, **ФЧХ**, **ГВЗ** — амплитудно-частотная и фазочастотная
  характеристики и групповое время запаздывания. У временных фильтров этих
  вкладок нет: они нелинейны либо нестационарны, и АЧХ у них не существует.
- **Импульсная** (цифровые) — отклик `h[n]`.
- **Сигнал** — сигнальная лаборатория, см. ниже.
- **Коэффициенты** — числа: отводы КИХ, биквады БИХ, номиналы деталей.
  Отсюда их переносят в свою программу или в схему.
- **Описание** — что получилось, словами: какой метод сработал, чем этот
  фильтр хорош, чем платите и на что смотреть. У временных фильтров здесь
  же объясняется их смысл — почему медиана убирает выброс бесследно, чем
  Калман отличается от альфа-бета-гаммы.

## 6. Сигнальная лаборатория (вкладка «Сигнал»)

Показывает фильтр в работе: на вход подаётся испытательный сигнал, на графике
рядом лежат **чистый**, **подан** (с шумом и помехами) и **фильтрован**.
Кнопками над графиком любой из них можно убрать с глаз.

- **Убрать задержку** — сдвигает выход на задержку фильтра, чтобы сравнивать
  форму, а не фазу. Число отсчётов написано на самой кнопке.
- **>> <<** и **<< >>** — сжать и растянуть время, **Сброс** — вернуть вид.
  График можно таскать мышью.
- **коридор** — полоса ±СКО вокруг чистого сигнала.
- Внизу — **СКО ошибки** и, для сравнения, СКО на входе: во сколько раз стало
  лучше (или хуже — программа так и напишет, «ХУЖЕ»).
- У Калмана отмечается отсчёт, на котором фильтр **сошёлся**.

Сигнал настраивается в меню **Сигнал → Параметры сигнала…**: частота и
амплитуда тона, амплитуда второй гармоники, частота и амплитуда помехи, СКО
белого шума, число выбросов и их амплитуда, длина записи в отсчётах (степень
двойки) и зерно шума. Зерно означает, что случайность **повторима**: с тем же
зерном сигнал тот же, и два фильтра сравниваются на одних и тех же данных.

**АМ** и **ЧМ** сигнала включаются не здесь, а прямо на панели параметров —
там же задаются глубина и частота модуляции.

## 7. Проекты `.sff`

В файле проекта лежит **спецификация — что просили**, а не результат: при
загрузке всё пересчитывается заново. Поэтому файл не может разойтись сам с
собой, а сам проект — это несколько строк обычного текста, которые не стыдно
положить в систему контроля версий и посмотреть глазами:

```
[Filter]
format=sff-1
kind=fir
shape=lowpass
fd=48000
fpass1=4000
fstop1=8000
ap_db=1
as_db=60
```

Готовые проекты лежат в каталоге `Examples/` — на каждый класс и на каждый
приём: равноволновый КИХ для звукового тракта, преобразователь Гильберта,
эллиптический полосовой БИХ, пассивная LC-лестница на 50 Ом, активный ПФ на
операционных усилителях, Калман по модели синусоиды и медианный против
выбросов. Открывайте и меняйте: это и есть самый быстрый способ освоиться.

У каждого в первых строках — комментарий, зачем он и на что смотреть.

## 8. Чего программа не делает

- Не обрабатывает файлы с записями: сигнальная лаборатория работает на
  СИНТЕЗИРОВАННОМ сигнале, который сама и порождает.
- Не выводит коэффициенты файлом для чужих программ — их берут глазами со
  вкладки «Коэффициенты».
- Не рисует схемы в формат САПР — чертёж на вкладке «Схема» для чтения
  человеком.
- Ряды номиналов (E24/E96) не применяются: номиналы даются точными.
